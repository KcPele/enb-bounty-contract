# Example environment variables for Base Mainnet deployment and verification

# The private key of your deployer wallet (keep this secret!)
PRIVATE_KEY=your_private_key_here

# Your BaseScan API key for contract verification (get from https://basescan.org)
BASESCAN_API_KEY=your_basescan_api_key_here

# The address that will receive royalties from ENBBountyNft (usually your treasury or multisig)
TREASURY_ADDRESS=your_treasury_address_here

# The authority address allowed to set ENBBounty contracts (often your admin or deployer address)
AUTHORITY_ADDRESS=your_authority_address_here

# Royalty fee in basis points (e.g., 500 = 5%) for ENBBountyNft
ROYALTY_FEE_NUMERATOR=500

# Initial claim index for ENBBounty (usually 0 unless you want to pre-fill claims)
START_CLAIM_INDEX=0

# Token addresses for multi-token support (set to empty or zero address if not using)
# USDC token contract address for Base Mainnet (optional - leave empty if not using USDC)
USDC_ADDRESS=your_usdc_address_here

# ENB token contract address (optional - leave empty if not using ENB token)
ENB_ADDRESS=your_enb_address_here 